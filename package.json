{
  "name": "aegis",
  "version": "1.0.0",
  "description": "AEGIS Personal Safety Intelligence Platform — root workspace",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:dashboard\"",
    "dev:backend": "cd backend && node src/index.js",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:dashboard": "cd dashboard && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../dashboard && npm install",
    "build:all": "cd frontend && npm run build && cd ../dashboard && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}